// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firestore: {
    apiKey: 'AIzaSyA5zsDTuFELyqlx_ZqqSecEOl9Yw8XoO1A',
    authDomain: 'control-clientes-bd82c.firebaseapp.com',
    databaseURL: 'https://control-clientes-bd82c.firebaseio.com',
    projectId: 'control-clientes-bd82c',
    storageBucket: 'control-clientes-bd82c.appspot.com',
    messagingSenderId: '1013212716894'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
